---
id: AIR-135.5
title: Cross-repo Arc contract——ai-guide↔SouthChariot ownership、shared identity＋開卡協議
status: To Do
assignee: []
created_date: '2026-09-18 10:04'
updated_date: '2026-09-18 11:33'
labels: []
dependencies:
  - AIR-135.3
  - AIR-135.4
parent_task_id: AIR-135
ordinal: 122000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
設計 AIR-135 program 的跨 repo 落地 contract。Backlog.md source 查證後，cross-repo 關係採 qualified text identity：每個 repo 保有自己的 canonical card tree；對應卡建立後，references[] 存對方 repo/task 座標，同一 token 逐字放進 Description 以便搜尋，https 僅作可點擊捷徑。parentTaskId 與 dependencies 都維持 repo-local，不承擔跨 repo 關係。ai-guide 擁有 Development Arc semantics、policy、Marshal/dispatch obligations 與 projection contract；SouthChariot 擁有人機 control surface、navigation、render/interaction，只讀 canonical state 並產 derived projection/cache，不能另存第二套 lifecycle truth。SC card 建立後再完成雙向 token 回鏈。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 定義 stable repo id 與 cross-repo token schema，例如 x-arc: ai-guide:AIR-135.5 <-> southchariot:SC-N (contract v1)；雙端 references[] 存 token、Description 同步 token 供 search；禁止跨 repo parentTaskId/dependencies。
- [ ] #2 定義 ownership matrix：ai-guide / Backlog card tree / code-fact producers / Code Lens / SouthChariot / Marshal 各自可讀、可寫、authoritative 的範圍；SC 只能持有 canonical identity/link 與 derived projection/cache。
- [ ] #3 定義 projection/action contract：SC 可讀哪些 canonical/derived state、哪些 human actions 必須回寫哪個 owner、衝突/stale 如何 fail-loud；https/blob URL 只作 convenience，不作 canonical identity。
- [ ] #4 SouthChariot implementation card readiness gate：本卡 contract 固定後先在 SC 建 repo-local card，SC card 先寫 reverse token 指回 AIR-135.5，再回 ai-guide 補 forward token；不得使用絕對本地路徑作 identity。
- [ ] #5 跨 repo dogfood：fresh session 僅憑 card 內 qualified token + repo-local 搜尋即可重建關係；任一 repo 暫時不可達時仍能辨識對端 identity，不需要人工 handoff 文件維持持久狀態。雙寫一致性（references[] 與 Description token 逐字一致）納入 dogfood 對帳，drift 即 fail-loud
- [ ] #6 cross-repo handoff 的 Arc/card/artifact/session identity 與 current verdict/context 由 Marshal 從 canonical state 自動組裝；SouthChariot SC-15/SC-16 的 messaging/router/receipt 能力負責 transport/delivery，AIR-135 不重造 courier protocol。user 不需複製 session id、verdict 或重新口述 context，transport identity 也不得成第二 lifecycle truth
- [ ] #7 projection/action contract 必能呈現 derived active-work/liveness/terminal status 與 pending-human-decision projection，且每項可追溯 authoritative owner；SC 可顯示與觸發 action，但不得靠 user 手動輪詢或在 SC 另存一套 arc state
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Muse source review：Backlog parent create 會在單一 project working-copy 查 parent，miss 即拒絕；dependencies 亦在 local corpus fail-closed；references 只做 trim/去空/去重，無 target existence/scheme 驗證，因此可承載 cross-repo token。但 task search 不索引 references，所以 token 必須同步進 Description/Notes；web 只把 http(s) render 成可點連結。MCP roots 同時只持有一個 active project。結論：不改 Backlog.md、不 upstream；現成 references + searchable text token + optional https 是目前 contract。
<!-- SECTION:NOTES:END -->
