# Marshal 增補修訂複核（唯讀）

repo `/Users/ctai/Github/ai-guide`，task `ai-analysis/_tasks/09-15-development-workflow-redesign/`。你是 Reviewer/findings，decision floor，review_findings qualification；無 apply/final disposition，禁止再委派。
讀 task 的 `ep.md`（目標骨架、S1新增末條、S2工單粒度、SM-20–24、增補帳本）與 `references/marshal-integration-review.json`。
僅核 M1–M4 的修訂／不採理由是否成立，是否留下可執行性或權限漏洞。尤其 M1：ordinary 三腿改單腿是本 EP 明示的新政策並帶對照實驗，不以現行三腿規則本身否決變更；檢查批次化能否偷掉新 profile 要求的独立性。
M2 核 followup row 與舊 verified/closed 寫入措辭的同步責任。M3 核逐 unit contract＋同 authority batch＋部分失敗。M4 核每 dispatch snapshot 與零輸出守衛。
必要時 rg 分段讀 current sources；不要重審原 F1–F6、不要讀 HTML、不要跑 tests。輸出四列 resolved/unresolved、錨點、理由；新 Important 附具體反例。全部 read-only，不寫檔／memory／git／部署／卡，不啟動其他 worker。由主 session 收回並更新帳本。
