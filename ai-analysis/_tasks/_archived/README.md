# _archived — 一次性遷入的歷史弧（凍結）

> AIR-77 一次遷入，之後凍結：可讀可引用，不再新增、不再搬移。

1. **legacy frozen**：本目錄只收 AIR-77 遷入的 33 弧（`done/` 31 弧＋AIR-45 兩已閉弧）；任務生命週期的唯一來源是卡片 status，不是目錄位置。
2. **新弧落 `_tasks/YYYY-MM/<MM-DD-主題>/`、永不搬移**：狀態只看卡片，路徑只表達 identity＋建立時間。
3. **本目錄僅一次性收入、不再變動**：後續退役走就地墓碑（檔頭 `superseded-by` 指標），不搬目錄。
